# 2.Indexer

Здесь будет консольная программа индексации.

План:
- чтение параметров из `config.ini` (каталоги, расширения);
- рекурсивный обход файлов;
- очистка текста и подсчёт частот слов;
- сохранение индексов в БД через модуль `1.DatabaseManager` (или его логику).
